# Realestate Chatbot — Full Stack Assignment (Generated)

This repository contains a full-stack example (Django REST + React) that implements the requirements described in the Sigmavalue assignment.

See `backend/` and `frontend/` for instructions to run each part locally.

The original assignment PDF is included in `docs/` inside this package.
